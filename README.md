TASKS PERFORMED::::

- Made the server
- Configure express and set up local host
- Configured express to accept JSON requests
- Move Harcoded Values like port number into variables
- Prepare the app so it can later read environment variables(After Deployment)
- Use express router to create /health to know whether the routes are working fine
- Set up the .env file in the root folder to store the mongodb connection string
- Put .env file in .gitignore
- Set up database and connect it server
- apply all the logs and checks to know anytime whether the db is working fine or not
- Write the password hashing code at the model/schema using pre(`save`) hook
-
